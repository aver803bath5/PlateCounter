import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    borderBottomWidth: 1,
    borderBottomColor: '#555',
    width: 200, 
    height: 26,
    fontSize: 20,
    color: '#000',
  },
});
